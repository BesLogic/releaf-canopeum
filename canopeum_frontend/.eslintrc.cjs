/** @type {import("eslint").Linter.Config} */
module.exports = {
  root: true,
  extends: [
    'beslogic/react',
    'beslogic/typescript',
    'beslogic/dprint',
  ],
  ignorePatterns: [
    // Because we set parserOptions.project
    '.eslintrc.cjs',
    // Auto-generated
    'src/services/api.ts',
  ],
  plugins: ['react-refresh'],
  // parser and parserOptions.project needs to be set explicitly because we explicitly set no-unsafe-*
  parser: '@typescript-eslint/parser',
  parserOptions: { project: ['tsconfig?(.*).json'] },
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
    /*
     * Beslogic presets overrides
     */
    // Our autogenerated API has lots of `any`. Let's at least be aware of it.
    '@typescript-eslint/no-unsafe-member-access': 'warn',
    '@typescript-eslint/no-unsafe-argument': 'warn',
    // For now, we're fine using relative paths instead of ts-paths in this project
    'no-autofix/no-relative-import-paths/no-relative-import-paths': 'off',
    // Using Bootraps directly without a React wrapper will cause us to have to add classes to React Components
    'react/forbid-component-props': 'off',
  },
}
